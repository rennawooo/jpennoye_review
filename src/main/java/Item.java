package main.java;public final class Item{        public int key;        //constructor        public Item(int keyValue) {                 key = keyValue;        }        public int getKey() {        	return key;        }                @Override        public boolean equals(Object compare) {        	if(compare == this) { //comparing Item to itself        		return true;        	}        	if(!(compare instanceof Item)){        		return false;        	}        	        	Item i = (Item) compare;        	return i.getKey() == this.key;        }}